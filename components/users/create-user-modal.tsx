"use client";
import { useState, type FormEvent } from "react";
import { X } from "lucide-react";
import { roleOptions, siteOptions, type UserRole } from "./users-data";
export function CreateUserModal({onClose}:{onClose:()=>void}){const[sendInvite,setSendInvite]=useState(true);const[role,setRole]=useState<UserRole>("Viewer");function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();onClose();}const inputClass="mt-1.5 h-10 w-full rounded-lg border bg-background px-3 text-sm outline-none transition-shadow focus:ring-2 focus:ring-ring/30";return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/55 p-4 backdrop-blur-[1px]" onMouseDown={e=>{if(e.target===e.currentTarget)onClose();}}><section role="dialog" aria-modal="true" aria-labelledby="create-user-title" className="w-full max-w-md overflow-hidden rounded-2xl border bg-card shadow-2xl"><header className="flex items-center justify-between border-b px-5 py-4"><h2 id="create-user-title" className="text-base font-bold">Create New User</h2><button type="button" onClick={onClose} className="grid size-8 place-items-center rounded-lg text-muted-foreground hover:bg-muted hover:text-foreground" aria-label="Close create user modal"><X className="size-4" /></button></header><form onSubmit={submit}><div className="space-y-4 p-5"><label className="block text-xs font-medium">Full Name <span className="text-destructive">*</span><input required placeholder="Jane Smith" className={inputClass}/></label><label className="block text-xs font-medium">Email Address <span className="text-destructive">*</span><input required type="email" placeholder="jane@divu.io" className={inputClass}/></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-xs font-medium">Role<select value={role} onChange={e=>setRole(e.target.value as UserRole)} className={inputClass}>{roleOptions.slice(1).map(option=><option key={option}>{option}</option>)}</select></label><label className="block text-xs font-medium">Primary Site<select defaultValue="" className={inputClass}><option value="">— None —</option>{siteOptions.slice(1).map(option=><option key={option}>{option}</option>)}</select></label></div><label className="flex items-center gap-3 rounded-xl border bg-muted/40 p-3 text-xs text-foreground"><input type="checkbox" checked={sendInvite} onChange={e=>setSendInvite(e.target.checked)} className="size-4 accent-[var(--primary)]"/>Send email invite — user sets their own password</label></div><footer className="flex gap-3 px-5 pb-5"><button type="button" onClick={onClose} className="h-10 flex-1 rounded-lg border text-sm font-semibold hover:bg-muted">Cancel</button><button type="submit" className="h-10 flex-1 rounded-lg bg-primary text-sm font-semibold text-primary-foreground hover:bg-primary/90">Create User</button></footer></form></section></div>}
