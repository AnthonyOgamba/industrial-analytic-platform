import { ChevronDown, Filter, Search, X } from "lucide-react";
import { roleOptions, siteOptions, statusOptions, type UserRole, type UserStatus } from "./users-data";
type Props = { query: string; site: string; role: UserRole | "All Roles"; status: UserStatus | "All"; resultCount: number; totalCount: number; onQueryChange: (value: string) => void; onSiteChange: (value: string) => void; onRoleChange: (value: UserRole | "All Roles") => void; onStatusChange: (value: UserStatus | "All") => void };
const selectClass = "h-9 appearance-none rounded-lg border bg-background py-1 pl-3 pr-8 text-xs outline-none transition-shadow focus:ring-2 focus:ring-ring/30";
export function UserFilters({ query, site, role, status, resultCount, totalCount, onQueryChange, onSiteChange, onRoleChange, onStatusChange }: Props) { const filters = [[site,siteOptions,onSiteChange],[role,roleOptions,onRoleChange],[status,statusOptions,onStatusChange]] as const; return <div className="flex flex-wrap items-center gap-2.5 rounded-xl border bg-card p-3 shadow-sm"><Filter className="size-4 shrink-0 text-muted-foreground" /><div className="relative min-w-[14rem] flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={e=>onQueryChange(e.target.value)} placeholder="Search name, email, role..." className="h-9 w-full rounded-lg border bg-background py-1 pl-9 pr-8 text-xs outline-none transition-shadow focus:ring-2 focus:ring-ring/30" />{query&&<button type="button" onClick={()=>onQueryChange("")} className="absolute right-2 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground" aria-label="Clear search"><X className="size-3.5" /></button>}</div>{filters.map(([value,options,setter])=><div className="relative" key={String(value)}><select value={value} onChange={e=>setter(e.target.value as never)} className={selectClass}>{options.map(option=><option key={option}>{option}</option>)}</select><ChevronDown className="pointer-events-none absolute right-2.5 top-1/2 size-3.5 -translate-y-1/2 text-muted-foreground" /></div>)}<span className="ml-auto whitespace-nowrap text-[11px] text-muted-foreground">{resultCount} of {totalCount} users</span></div>; }
