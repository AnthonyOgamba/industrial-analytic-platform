"use client";

import { PlugZap, Save } from "lucide-react";
import { useState, type FormEvent } from "react";
import { sourceTypes, type IndustrialDataSource, type SourceStatus, type SourceType } from "./data-input-data";
import { DataInputModalShell } from "./data-input-modal-shell";

const fieldClass = "h-10 w-full rounded-lg border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring/30";
const labelClass = "space-y-1.5 text-xs font-medium";

export function EditDataSourceModal({ source, onClose, onSave }: { source: IndustrialDataSource; onClose: () => void; onSave: (source: IndustrialDataSource) => void }) {
  const [form, setForm] = useState(() => ({ ...source }));
  const [testMessage, setTestMessage] = useState("");
  function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); onSave(form); }
  const footer = <><button type="button" onClick={onClose} className="h-10 rounded-lg border px-4 text-sm font-semibold hover:bg-muted">Cancel</button><button type="submit" form="edit-data-source-form" className="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-primary px-5 text-sm font-semibold text-primary-foreground hover:bg-primary/90"><Save className="size-4" />Save Changes</button></>;
  return <DataInputModalShell title={`Edit Source — ${source.name}`} subtitle="Configure a secured industrial data source for telemetry ingestion." onClose={onClose} footer={footer}><form id="edit-data-source-form" onSubmit={submit} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><label className={labelClass}>Source Name<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className={fieldClass} /></label><label className={labelClass}>Source Type<select value={form.type} onChange={(event) => setForm({ ...form, type: event.target.value as SourceType })} className={fieldClass}>{sourceTypes.map((type) => <option key={type}>{type}</option>)}</select></label><label className={labelClass}>Facility / Plant<input required value={form.facility} onChange={(event) => setForm({ ...form, facility: event.target.value })} className={fieldClass} /></label><label className={labelClass}>Dataset Produced<input required value={form.dataset} onChange={(event) => setForm({ ...form, dataset: event.target.value })} className={fieldClass} /></label><label className={`${labelClass} sm:col-span-2`}>Description<textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} className="min-h-24 w-full rounded-lg border bg-background p-3 text-sm" /></label></div><fieldset className="rounded-xl border bg-muted/30 p-4"><legend className="px-1 font-mono text-[10px] font-semibold uppercase tracking-[0.12em] text-primary">Connection Settings</legend><div className="mt-2 grid gap-4 sm:grid-cols-2"><label className={labelClass}>Endpoint / Host<input value={form.endpoint} onChange={(event) => setForm({ ...form, endpoint: event.target.value })} className={fieldClass} /></label><label className={labelClass}>Authentication Method<select value={form.authenticationMethod} onChange={(event) => setForm({ ...form, authenticationMethod: event.target.value })} className={fieldClass}>{["API Key", "Client Certificate", "OAuth 2.0", "Service Account", "Bearer Token", "Signed Upload"].map((item) => <option key={item}>{item}</option>)}</select></label><label className={labelClass}>Sync Frequency<select value={form.syncFrequency} onChange={(event) => setForm({ ...form, syncFrequency: event.target.value })} className={fieldClass}>{["Real-time", "Every 30 seconds", "Every 5 minutes", "Every 15 minutes", "Daily", "Monthly"].map((item) => <option key={item}>{item}</option>)}</select></label><label className={labelClass}>Initial Status<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as SourceStatus })} className={fieldClass}><option>Connected</option><option>Offline</option><option>Error</option></select></label></div><div className="mt-4 flex flex-wrap gap-4"><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={form.encryption} onChange={(event) => setForm({ ...form, encryption: event.target.checked })} className="size-4 accent-[var(--primary)]" />Enable Encryption</label><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={form.validation} onChange={(event) => setForm({ ...form, validation: event.target.checked })} className="size-4 accent-[var(--primary)]" />Enable Pre-Import Validation</label></div><button type="button" onClick={() => setTestMessage("Mock connection test passed; no credentials or network request were used.")} className="mt-4 inline-flex h-9 items-center gap-2 rounded-lg border px-3 text-xs font-semibold hover:bg-muted"><PlugZap className="size-3.5" />Test Connection</button>{testMessage && <p className="mt-3 text-xs text-[var(--dv-badge-ok-text)]">{testMessage}</p>}</fieldset></form></DataInputModalShell>;
}
