"use client";

import { useEffect, useState, type FormEvent, type ReactNode } from "react";
import { Building2, X } from "lucide-react";
import { apiRequest } from "@/lib/api-client";
import type { FacilityManagerOption } from "@/lib/page-contracts";
import { emptyFacilitySettings, facilitySettingsPayload, type FacilitySettings } from "@/lib/facility-contract";
export { emptyFacilitySettings, facilitySettingsPayload, type FacilitySettings } from "@/lib/facility-contract";

export type FacilityFieldErrors=Partial<Record<keyof FacilitySettings,string>>;

const input="mt-1.5 h-10 w-full rounded-lg border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring/30";
function Section({title,children}:{title:string;children:ReactNode}){return <fieldset className="space-y-3 rounded-xl border p-4"><legend className="px-1 font-mono text-xs font-semibold uppercase tracking-wider text-primary">{title}</legend><div className="grid gap-3 sm:grid-cols-2">{children}</div></fieldset>}
function Field({name,label,error,children,wide=false}:{name:keyof FacilitySettings;label:string;error?:string;children:ReactNode;wide?:boolean}){return <label className={`text-xs font-medium ${wide?"sm:col-span-2":""}`} htmlFor={`facility-${name}`}>{label}{children}{error&&<span id={`facility-${name}-error`} className="mt-1 block text-xs text-destructive">{error}</span>}</label>}

export function FacilitySettingsFields({form,change,managers,policies,errors={}}:{form:FacilitySettings;change:<K extends keyof FacilitySettings>(key:K,value:FacilitySettings[K])=>void;managers:FacilityManagerOption[];policies:string[];errors?:FacilityFieldErrors}){
  const attrs=(name:keyof FacilitySettings)=>({id:`facility-${name}`,name,"aria-invalid":Boolean(errors[name]),"aria-describedby":errors[name]?`facility-${name}-error`:undefined});
  return <>
    <Section title="Identity"><Field name="name" label="Name" error={errors.name}><input {...attrs("name")} required value={form.name} onChange={event=>change("name",event.target.value)} className={input}/></Field><Field name="code" label="Code" error={errors.code}><input {...attrs("code")} required value={form.code} onChange={event=>change("code",event.target.value)} className={input}/></Field><Field name="status" label="Status" error={errors.status}><select {...attrs("status")} value={form.status} onChange={event=>change("status",event.target.value)} className={input}><option value="active">active</option><option value="inactive">inactive</option></select></Field><Field name="managerUserId" label="Manager" error={errors.managerUserId}><select {...attrs("managerUserId")} required value={form.managerUserId} onChange={event=>change("managerUserId",event.target.value)} className={input}><option value="">Select manager</option>{managers.map(manager=><option key={manager.uid} value={String(manager.uid)}>{manager.username} · {manager.role}</option>)}</select></Field><Field name="description" label="Description" error={errors.description} wide><textarea {...attrs("description")} value={form.description} onChange={event=>change("description",event.target.value)} className="mt-1.5 min-h-20 w-full rounded-lg border bg-background p-3 text-sm"/></Field></Section>
    <Section title="Structured location">{([['address','Address'],['city','City'],['provinceState','Province / state'],['country','Country'],['countryCode','Country code'],['timezone','Timezone'],['latitude','Latitude'],['longitude','Longitude']] as const).map(([name,label])=><Field key={name} name={name} label={label} error={errors[name]}><input {...attrs(name)} required={['city','country','countryCode','timezone'].includes(name)} type={name==='latitude'||name==='longitude'?'number':'text'} step="any" maxLength={name==='countryCode'?2:undefined} value={form[name]} onChange={event=>change(name,event.target.value)} className={input}/></Field>)}</Section>
    <Section title="Governance & financial"><Field name="governancePolicy" label="Governance policy" error={errors.governancePolicy}><select {...attrs("governancePolicy")} required value={form.governancePolicy} onChange={event=>change("governancePolicy",event.target.value)} className={input}><option value="">Select active policy</option>{policies.map(policy=><option key={policy} value={policy}>{policy}</option>)}</select>{!policies.length&&<span className="mt-1 block text-xs text-amber-600">Create an active policy in Governance, then reopen this form.</span>}</Field><Field name="accessLevel" label="Default access level" error={errors.accessLevel}><select {...attrs("accessLevel")} value={form.accessLevel} onChange={event=>change("accessLevel",event.target.value)} className={input}>{["viewer","operator","manager","admin"].map(level=><option key={level}>{level}</option>)}</select></Field>{([['securityZone','Security zone'],['currency','Currency'],['hourlyProductionValue','Hourly production value'],['downtimeCostPerHour','Downtime cost per hour']] as const).map(([name,label])=><Field key={name} name={name} label={label} error={errors[name]}><input {...attrs(name)} required={name==='currency'} type={name==='hourlyProductionValue'||name==='downtimeCostPerHour'?'number':'text'} min={name==='hourlyProductionValue'||name==='downtimeCostPerHour'?0:undefined} step={name==='hourlyProductionValue'||name==='downtimeCostPerHour'?'0.01':undefined} maxLength={name==='currency'?3:undefined} value={form[name]} onChange={event=>change(name,event.target.value)} className={input}/></Field>)}</Section>
  </>;
}

export function FacilityEditModal({facilityId,managers,governancePolicies,onClose,onSaved}:{facilityId:number;managers:FacilityManagerOption[];governancePolicies:string[];onClose:()=>void;onSaved:()=>Promise<void>}){const[form,setForm]=useState(emptyFacilitySettings),[loading,setLoading]=useState(true),[pending,setPending]=useState(false),[error,setError]=useState("");const change=<K extends keyof FacilitySettings>(key:K,value:FacilitySettings[K])=>setForm(current=>({...current,[key]:value}));useEffect(()=>{let active=true;void apiRequest<unknown>(`/api/backend/facilities/${facilityId}`).then(payload=>{const outer=payload&&typeof payload==='object'?payload as Record<string,unknown>:{},record=outer.data&&typeof outer.data==='object'?outer.data as Record<string,unknown>:outer;if(active)setForm(Object.fromEntries((Object.keys(emptyFacilitySettings) as (keyof FacilitySettings)[]).map(key=>[key,record[key]==null?emptyFacilitySettings[key]:String(record[key])])) as FacilitySettings)}).catch(cause=>active&&setError(cause instanceof Error?cause.message:"Facility details could not be loaded.")).finally(()=>active&&setLoading(false));return()=>{active=false}},[facilityId]);async function submit(event:FormEvent){event.preventDefault();setPending(true);setError("");try{await apiRequest(`/api/backend/facilities/${facilityId}`,{method:"PATCH",body:JSON.stringify(facilitySettingsPayload(form))});await onSaved();onClose()}catch(cause){setError(cause instanceof Error?cause.message:"Facility changes could not be saved.")}finally{setPending(false)}}return <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/65 sm:items-center sm:p-5"><section role="dialog" aria-modal="true" className="max-h-[95vh] w-full max-w-4xl overflow-y-auto rounded-2xl border bg-background"><header className="sticky top-0 z-10 flex gap-3 border-b bg-background p-5"><Building2/><div className="flex-1"><h2 className="font-bold">Edit facility</h2><p className="text-xs text-muted-foreground">Update persisted facility settings. Halls, lines, and stations remain on their own domain endpoints.</p></div><button type="button" onClick={onClose} aria-label="Close"><X/></button></header>{loading?<div className="m-5 h-72 animate-pulse rounded-xl bg-muted"/>:<form onSubmit={submit} className="space-y-4 p-5"><FacilitySettingsFields form={form} change={change} managers={managers} policies={governancePolicies}/>{error&&<p role="alert" className="text-destructive">{error}</p>}<footer className="flex justify-end gap-2 border-t pt-4"><button type="button" onClick={onClose} className="h-10 rounded-lg border px-4">Cancel</button><button disabled={pending} className="h-10 rounded-lg bg-primary px-4 text-primary-foreground">{pending?"Saving…":"Save facility"}</button></footer></form>}</section></div>}
